const multipul = (a,b,c) =>{
    const mulnum =a*b*c;
    return mulnum;
}

console.log(multipul(2,3,4));

const threeline = 
`
i am a web devloper.
i love to code.
i love to eat birayani.
`

console.log(threeline);

function twoparamert(a, b=3)
{
    const result = a+b;
    return result;
}

console.log(twoparamert(4));