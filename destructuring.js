// direct destructing ar maddome access kore 
const {age,name} = {name: 'dolon', age: 45, profession:'student'};

console.log(age);

// object ar maddome access kore
const fish = {
    nameFish:'ilsh',
    price: 3000,
    country:'bangladesh',
    color:'lal'

}
const {nameFish,color} = fish;
console.log(nameFish);

// c
const array = [10,20,40,50];
const [ageA ,numver] = array;
console.log(ageA);
